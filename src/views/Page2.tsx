import { defineComponent } from '@vue/composition-api'

export default defineComponent({
  setup () {
    return () => (
      <div style={{ textAlign: 'center', fontSize: '24px' }}>simple page2</div>
    )
  }
})
